# Function: createLogger()

```ts
function createLogger(context): Logger;
```

Defined in: [niuma/log/index.ts:44](https://github.com/lihaizhong/niuma/blob/main/niuma/log/index.ts#L44)

创建带上下文的 logger

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `context` | `string` | 上下文名称（如模块名） |

## Returns

`Logger`

带上下文的 logger 实例
